/** @jsx React.DOM */

var React = require('react');

var Search = React.createClass({

    render: function() {
        return (
            <h1>Search</h1>
        );
    }
});

module.exports = Search;
