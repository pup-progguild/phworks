/** @jsx React.DOM */

var React = require('react');

var Register = React.createClass({

    render: function() {
        return (
            <h1>Register</h1>
        );
    }
});

module.exports = Register;
